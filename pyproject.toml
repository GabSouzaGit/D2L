[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "D2L"
version = "0.1.0"
description = "Arquivo digital de D2L: As Aventuras de Diana, Dena e Lacey"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]